// 项目名称
const APP_NAME = 'Weapp Template'

// 项目Slogon
const APP_SLOGON = '微信小程序通用代码'

// 项目主题色
const APP_BRAND_COLOR = '#27cfbe'

// 本地图片资源路径
const LOCAL_IMAGE_DIR = '/assets'

// 数据请求分页
const DEFAULT_COUNT = 10

module.exports = {
  APP_NAME,
  APP_SLOGON,
  APP_BRAND_COLOR,
  LOCAL_IMAGE_DIR,
  DEFAULT_COUNT,
}